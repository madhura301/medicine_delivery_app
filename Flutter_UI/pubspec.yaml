name: medicine_delivery_app
description: A Flutter app for medicine delivery service

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  # shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0 
  # Location & Maps
  geolocator: ^9.0.2
  geocoding: ^2.1.1
  # UI & Icons
  cupertino_icons: ^1.0.2
  
  # State Management
  flutter_bloc: ^8.1.3
  
  # Network & Storage
  dio: ^5.3.2
  shared_preferences: ^2.2.2
  
  # Utils
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/icons/